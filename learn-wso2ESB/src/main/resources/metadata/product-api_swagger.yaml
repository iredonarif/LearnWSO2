openapi: 3.0.1
info:
  title: product-api
  description: API Definition of product-api
  version: 1.0.0
servers:
- url: http://localhost:8290/product
paths:
  /add:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
  /add-many:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
  /all:
    get:
      responses:
        default:
          description: Default response
